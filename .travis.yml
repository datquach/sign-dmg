language: node_js
os: osx
addons:
  artifacts:
    target_paths: dmgfiles/*
    debug: true
jobs:  
  include:
    - stage: "CreateDMG"
      name: "Creating"
      script: 
        - npm install --global create-dmg
        - create-dmg 'KatalonStudio.app' dmgfiles/